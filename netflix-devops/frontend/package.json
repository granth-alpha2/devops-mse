{
  "name": "netflix-frontend",
  "version": "1.0.0",
  "description": "Netflix-style streaming platform frontend",
  "main": "index.js",
  "scripts": {
    "start": "react-scripts start",
    "dev": "react-scripts start",
    "build": "react-scripts build",
    "test": "CI=true react-scripts test --watchAll=false --passWithNoTests",
    "test:coverage": "CI=true react-scripts test --watchAll=false --coverage --passWithNoTests",
    "eject": "react-scripts eject",
    "lint": "echo \"frontend lint not configured\"",
    "format": "echo \"frontend format not configured\""
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "react-router-dom": "^6.11.0",
    "axios": "^1.4.0",
    "prop-types": "^15.8.1"
  },
  "devDependencies": {
    "react-scripts": "5.0.1",
    "eslint": "^8.40.0"
  }
}
